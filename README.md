# Telegram Mini App — Программа работ 2026

Мини-приложение для просмотра программы ремонта дорог прямо в Telegram.
Данные берутся напрямую из Google Таблицы и обновляются автоматически каждые 5 минут.

## Структура проекта

```
tg-miniapp/
├── bot.py              ← Telegram-бот (Python)
├── requirements.txt
└── webapp/
    └── index.html      ← Само Mini App (хостится на любом HTTPS-хостинге)
```

---

## Шаг 1 — Подготовить Google Таблицу

1. Откройте вашу таблицу в Google Sheets.
2. **Файл → Поделиться → Настроить доступ** → выберите
   *"Все, у кого есть ссылка"* → уровень **Читатель**.
3. Скопируйте ID таблицы из адресной строки:
   ```
   https://docs.google.com/spreadsheets/d/  <<<ВОТ_ЭТО>>>  /edit
   ```
4. В файле `webapp/index.html` найдите строку и вставьте ID:
   ```js
   const SHEET_ID   = "YOUR_GOOGLE_SHEET_ID";
   const SHEET_NAME = "Лист1";   // имя вкладки
   const HEADER_ROW = 2;         // номер строки с заголовками
   ```
5. Проверьте маппинг столбцов (`const COL = { … }`) — индексы 0-based
   (A=0, B=1, C=2 …). Подгоните под вашу таблицу.

---

## Шаг 2 — Захостить Mini App (бесплатно)

Нужен **HTTPS**. Самый простой вариант — **GitHub Pages**:

1. Создайте репозиторий на GitHub (публичный).
2. Загрузите файл `webapp/index.html` как `index.html` в корень репо.
3. Перейдите **Settings → Pages → Source: main / root** → Save.
4. Через минуту получите URL вида `https://<username>.github.io/<repo>/`.

Альтернативы: [Netlify Drop](https://app.netlify.com/drop),
[Vercel](https://vercel.com), любой VPS с nginx.

---

## Шаг 3 — Создать бота и получить токен

1. Откройте [@BotFather](https://t.me/BotFather) → `/newbot`.
2. Введите имя и username бота.
3. Скопируйте токен.
4. Вставьте его в `bot.py`:
   ```python
   BOT_TOKEN = "1234567890:AAF…"
   WEBAPP_URL = "https://username.github.io/repo/"
   ```

---

## Шаг 4 — Запустить бота

```bash
pip install -r requirements.txt
python bot.py
```

Напишите боту `/start` — появится кнопка **📋 Программа работ 2026**.

---

## Как работает синхронизация

| Действие | Поведение |
|---|---|
| Открытие приложения | Автоматическая загрузка данных |
| Кнопка **Sync** | Принудительное обновление |
| Каждые 5 минут | Фоновое авто-обновление |

Приложение читает данные через Google Visualization API (`gviz/tq`) —
авторизация не нужна, работает из браузера напрямую.

---

## Настройка столбцов

В `index.html` объект `COL` задаёт 0-based индексы столбцов таблицы:

```js
const COL = {
  num:        0,   // A — №
  title:      1,   // B — Наименование объекта
  length:     2,   // C — Протяжённость, км
  contract:   3,   // D — № Контракта
  pto:        4,   // E — Исполнитель ПТО
  kameral:    5,   // F — Камерал
  ourOrder:   6,   // G — Наш приказ
  sigForeman: 7,   // H — Подписант Прораб
  sigGeo:     8,   // I — Подписант ГЕО
  sigMain:    9,   // J — Подписант Глав. СК
  sigRks:     10,  // K — Подписант РКС СК
  rksOrder:   11,  // L — РКС приказ
};
```
